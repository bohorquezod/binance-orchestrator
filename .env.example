# Binance DB API MCP - Environment Variables Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# API Configuration
# =============================================================================

# Base URL of the Binance DB API (including the /api prefix)
# Default: http://localhost:3000/api
# Production: http://api.borasta.sytes.net/binance-db-api/api
BINANCE_DB_API_URL=http://api.borasta.sytes.net/binance-db-api/api

# Full URL to the OpenAPI specification JSON endpoint
# Default: Automatically built from BINANCE_DB_API_URL
# Production: http://api.borasta.sytes.net/binance-db-api/api-docs.json
OPENAPI_SPEC_URL=http://api.borasta.sytes.net/binance-db-api/api-docs.json

# Full URL to the database schema JSON endpoint
# Default: Automatically built from BINANCE_DB_API_URL
# Production: http://api.borasta.sytes.net/binance-db-api/api/db/schema
DB_SCHEMA_URL=http://api.borasta.sytes.net/binance-db-api/api/db/schema

# Timeout for API requests in milliseconds
# Default: 30000 (30 seconds)
BINANCE_DB_API_TIMEOUT=30000

# =============================================================================
# Transport Configuration
# =============================================================================

# Transport mode: 'stdio' or 'http'
# Default: http
# Use 'stdio' for local development with Cursor
# Use 'http' for server deployment
BINANCE_DB_API_TRANSPORT=http

# =============================================================================
# HTTP Server Configuration (only used when TRANSPORT=http)
# =============================================================================

# Host for the HTTP server
# Default: 0.0.0.0
BINANCE_DB_API_HTTP_HOST=0.0.0.0

# Port for the HTTP server
# Default: 8080
# Can also be set via PORT environment variable
BINANCE_DB_API_HTTP_PORT=8080

# Base path for the HTTP server
# Default: /
BINANCE_DB_API_HTTP_PATH=/

# Health check endpoint path
# Default: /health
BINANCE_DB_API_HTTP_HEALTH_PATH=/health

# =============================================================================
# Examples for different environments
# =============================================================================

# Local Development:
# BINANCE_DB_API_URL=http://localhost:6300/api
# OPENAPI_SPEC_URL=http://localhost:6300/api-docs.json
# DB_SCHEMA_URL=http://localhost:6300/api/db/schema
# BINANCE_DB_API_TRANSPORT=stdio

# Production:
# BINANCE_DB_API_URL=http://api.borasta.sytes.net/binance-db-api/api
# OPENAPI_SPEC_URL=http://api.borasta.sytes.net/binance-db-api/api-docs.json
# DB_SCHEMA_URL=http://api.borasta.sytes.net/binance-db-api/api/db/schema
# BINANCE_DB_API_TRANSPORT=http
